// License purchase UI removed in this custom build (all features unlocked locally).
export const GetLicense = (_props?: {
    setState?: any;
    buttonText?: string;
    buttonClassName?: string;
}) => null;
